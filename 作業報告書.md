# 作業報告書

## 2026-08-29 初期構築

### 今回変更した内容

- VReviewプロジェクトを初期化
- これまでの要件定義をREADME.mdへ整理
- GitHub Pages前提の静的Web構成を採用
- ChatGPT Plusへ手動で解析パッケージを渡す方式を正式仕様として記載
- AIM + Movement採点、Combat Scene自動検出 + 手動修正、30/60fpsコンタクトシート方式を仕様化
- Dashboard / New Review / Import Result / History / Training / Settings のページ土台を追加
- MP4 / WebMのブラウザ内読み込み処理を追加
- 動画時間・解像度・ファイルサイズ表示を追加
- Combat Sceneを手動で開始/終了指定して追加・編集・削除できる初期UIを追加
- Scene範囲をタイムライン上に表示する初期機能を追加
- localStorage用の軽量保存ヘルパーを追加

### 変更したファイル

- README.md
- 作業報告書.md
- index.html
- review.html
- result.html
- history.html
- training.html
- settings.html
- css/base.css
- css/layout.css
- css/components.css
- js/app.js
- js/video.js
- js/ui.js
- js/storage.js

### 追加した機能

- ダーク基調のVReview UI土台
- 動画ドラッグ&ドロップ / ファイル選択
- 動画プレビュー
- 動画メタ情報表示
- Scene開始・終了位置指定
- Scene追加・削除・簡易調整
- Sceneのタイムライン表示
- レスポンシブな基本レイアウト

---

## 2026-08-29 Combat Scene自動検出

### 今回変更した内容

- `js/scene-detection.js` を新規追加
- 外部APIを使わずブラウザ内でCombat Scene候補を検出する処理を追加
- Web Audio APIで約0.05秒単位のRMS・ピーク・立ち上がりを解析
- Canvasへ低解像度フレームを描画し、画面全体と中央領域のフレーム差分を解析
- 音声スコアと映像変化スコアを合成して戦闘イベント候補を生成
- 約1.5秒以内の近接イベントを同一Sceneとしてまとめる処理を追加
- Scene開始を最初の候補約0.7秒前、終了を最後の候補約0.8秒後へ広げる処理を追加
- 約6秒を超える長い候補を分割する初期処理を追加
- 検出感度「低い / 標準 / 高い」を追加
- 自動検出SceneへHIGH / MEDIUM / LOWのConfidence表示を追加
- 自動検出後も開始・終了を手動調整できる既存操作を維持
- Scene開始・終了の両方向0.1秒調整ボタンを追加
- 自動検出をやり直す際、既存Sceneを置換する前に確認を出すようにした

### 変更したファイル

- js/scene-detection.js
- js/app.js
- js/ui.js
- review.html
- css/components.css
- README.md
- 作業報告書.md

### 修正した不具合 / 改善

- 自動検出前にSceneが空のとき、何をすればよいか分かりにくかったため空状態メッセージを追加
- Scene調整が「開始を前へ」「終了を後ろへ」だけだったため、開始・終了とも前後へ0.1秒調整できるよう改善
- 自動検出結果と手動追加Sceneを見分けやすくするため表示を追加

### 追加した機能

- Combat Scene自動検出
- 検出感度切り替え
- 解析進捗表示
- Confidence表示
- 自動検出やり直し
- 音声解析失敗時の映像のみフォールバック

### 検出方式の注意点

現在の自動検出はVALORANT専用の学習済みAIではなく、音声トランジェントと映像差分を使うヒューリスティック方式です。

そのため以下は誤検出の可能性があります。

- スキル使用時の大きな効果音
- 爆発音
- 急な180度ターン
- ラウンド切り替え等の大きな画面変化

一方で、銃声が小さい・無音録画・画面変化が少ない場面は見逃す可能性があります。自動結果は候補として扱い、ユーザー確認と手動修正を前提とします。

### 未完了の項目

- Scene結合 / 分割の手動操作
- 自動30 / 60fps判定
- フレーム抽出
- Overview / Detailコンタクトシート生成
- ChatGPT解析パッケージZIP生成
- prompt自動生成
- result JSON解析 / Schema検証
- Review Result本画面
- IndexedDBによる履歴保存
- Training集計
- Settingsフォーム

### 確認できたこと

- `js/scene-detection.js` のJavaScript構文チェック
- `js/app.js` のJavaScript構文チェック
- `js/ui.js` のJavaScript構文チェック
- GitHub上で必要ファイルを作成・更新できたこと

### 確認できなかったこと

- 実際のVALORANT動画での検出精度
- GitHub Pages上での自動検出実動作
- Firefox / Chrome間のAudioContext / MP4デコード差
- 10〜60秒クリップでの実解析時間
- 長いクリップでのメモリ使用量
- 音声なし動画での実用精度

### 今後必要な作業

1. 実際のVALORANTクリップで自動検出精度を確認
2. 誤検出・見逃しを見て閾値と重みを調整
3. Scene結合 / 分割操作を追加
4. 30 / 60fps自動判定を実装
5. フレーム抽出・コンタクトシート生成を実装
6. 解析パッケージZIP生成
7. AI返却JSONの検証・表示
8. 履歴 / Training / Settingsを実装
