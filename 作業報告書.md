# 作業報告書

## 2026-08-31 Web Project Guide v1.8.0 準拠改修 — VReview v0.6.0

### Project Profile

- VReview: **v0.6.0**
- Detector: **v0.5.0**
- Feedback Package: **v5**
- Storage Schema: **v1**
- Feedback Handoff Schema: **v1**
- Diagnostics Schema: **v1**
- Web Project Guide: **v1.8.0**
- Profiles: **STATIC + MEDIA + AI-HANDOFF + TOOL**

Detectorの閾値・Scene判定条件は今回変更していない。Detector精度比較では引き続きDetector v0.5.0として扱う。

### 確認した最新Guide

`EliteMay/web-project-guide` mainを再確認し、Guide Version正本が**v1.8.0 / active / 2026-08-31**であることを確認した。

`README.md` / `START_HERE.md`から、既存Interactive Web Toolの更新に必要な範囲だけ選択した。

今回特に適用した最新版ルール:

- `PROJECT_LEARNINGS.md`をProject Memoryの正本として持つ
- 高コストBugはRoot Cause / Regression Guard / Preventionまで残す
- Interactive ProjectはDevelopment Diagnosticsを持つ
- Error / Unhandled Rejection / Storage / Import / Network failureをEvidenceとして残す
- Breadcrumbを上限付きRing Bufferで保存
- Diagnosticsへ秘密情報・ユーザー入力全文・Media Bodyを記録しない
- DiagnosticsをLocal-firstにし、外部へ自動送信しない
- One-click Diagnostic Export
- Error IDで画面上Errorと診断Logを対応付ける
- AI Coding Agent用`AGENTS.md`は薄いRouterとし、Specを複製しない
- 最終Commit / Merge CommitでCI / Pagesを再確認する

### 今回追加したもの

#### `PROJECT_LEARNINGS.md`

VReviewで既に発生した高コスト問題を長期知識として整理した。

Failure:

- Versioned Detector Patch積層
- 外部JSZip CDN依存によるFeedback ZIP作成失敗
- 誤検出除去の強化で本物のKill SceneをHard DropしたRecall低下
- Static ValidationとBrowser Validationの混同

Success:

- `primary / weak`二段階Scene分類
- 中央動画固定 + 右Scene PaneのみScroll
- Feedback Packageに正解Label / Detector diagnostics / ROI画像をまとめる方式

今後は高コストBugを単なる作業履歴へ埋めず、Root Cause / Regression Guard / Preventionをここへ残す。

#### `AGENTS.md`

Coding Agent向けの入口を追加。

- README / SPEC / PROJECT_LEARNINGS / 作業報告 / latest GuideへRouting
- Canonical file ownership
- High-risk area
- Non-breakable rules
- Static / Regression command
- Completion条件

仕様全文はAGENTSへ重複していない。

#### Development Diagnostics

新規:

- `diagnostics.html`
- `js/diagnostics.js`
- `css/diagnostics.css`
- `data/diagnostics-schema.json`

DiagnosticsはこのTabの`sessionStorage`へ保存する。

上限:

- Breadcrumb: 120
- Runtime Error: 40
- Network failure: 30

記録:

- App / Build / Storage Schema / Detector / Feedback / Guide Version
- Route / Session / viewport / language / online
- Browser / Platformの最小Summary
- Feature Detection
- 動画読込結果
- Draft / Backup復元
- Detector Start / Success / Cancel / Failure
- Feedback Export Start / Success / Failure
- Detector Test Import / Schema failure
- JavaScript Error / Unhandled Promise Rejection
- Storage failure / tab conflict
- sanitized Network failure
- Storage件数 /概算容量Summary

記録しない:

- 元動画・画像・音声Body
- Scene内容本体
- Feedbackメモ本文
- File名 / Path
- localStorage / sessionStorage値本体
- API Key / Token / Password / Cookie / Authorization

Diagnosticsは自動送信せず、必要な場合だけ`diagnostics.json`として書き出す。

### Error ID

主要なcatch経路へError IDを追加した。

- `MEDIA-LOAD-001`
- `DETECTOR-RUN-001`
- `FEEDBACK-EXPORT-001`
- `DETECTOR-TEST-SCHEMA-001`
- `DETECTOR-TEST-IMPORT-001`
- `STORAGE-001`
- `JS-UNEXPECTED-001`
- `JS-PROMISE-001`
- `DIAGNOSTICS-EXPORT-001`
- `DIAGNOSTICS-COPY-001`

動画読込・Detector・Feedback Export失敗は画面へError IDを表示し、同じIDをDiagnosticsから追えるようにした。

### UI / Navigation

通常Navigation:

- Dashboard
- New Review
- Detector Test
- Diagnostics

未実装のResult / History / Training / Settingsは引き続きDevelopment扱い。

New Reviewの主要仕様:

```text
左Navigation固定
中央動画固定
右Scene Paneのみ縦Scroll
```

は変更していない。

### Version / Source of Truth

`js/version.js`:

- App: `0.6.0`
- Detector: `0.5.0`
- Feedback: `5`
- Storage Schema: `1`
- Feedback Schema: `1`
- Diagnostics Schema: `1`
- Build: `20260831-1`
- Guide: `1.8.0`

`project-meta.json`へ:

- `PROJECT_LEARNINGS.md`
- `AGENTS.md`
- Diagnostics Runtime / View / Policy

を追加。

### Browser Checklist

`tests/BROWSER_CHECKLIST.md`をGuide v1.8.0へ更新。

追加確認:

- Error ID
- Diagnostics Navigation
- Breadcrumb
- Runtime Error
- Storage Error
- diagnostics.json Export / Copy / Clear
- Privacy確認
- Ring Buffer上限
- Detector Test failure diagnostics

Checklistを更新しただけではBrowser Validated扱いにしない。

---

## Compatibility

### 維持

- GitHub Pages公開方式
- `index.html`
- `review.html`
- `detector-test.html`
- Detector v0.5.0判定条件
- Feedback Package v5
- Detector Test v4 / v5 Import
- Storage Schema v1
- v0.5.0以前のDraft読込
- New Reviewの固定動画 + 右Pane Scroll

### 追加

- `diagnostics.html`
- Diagnostics Schema v1
- `PROJECT_LEARNINGS.md`
- `AGENTS.md`

既存ユーザー保存データのMigrationは発生しない。

---

## Verification State

### Implemented — **PASS**

- Latest Guide v1.8.0 metadata
- PROJECT_LEARNINGS
- AGENTS Router
- Local Development Diagnostics
- Error IDs
- Breadcrumbs
- Diagnostic Export / Copy / Clear
- Diagnostics privacy / Ring Buffer
- Active page Diagnostics wiring
- Browser Checklist更新

### Pull Request Validation — **PASS**

PR #1 `feat: align VReview with web-project-guide v1.8.0` を作成し、head commit `5b1c8fd2876211cff8c4eaa591597cf83532f0a9` に対する `Validate VReview` が成功した。

- JavaScript / MJS syntax: success
- Project structure / references / Guide / Diagnostics validation: success
- Storage regression tests: success

PRは同じhead commitを確認してSquash Mergeした。

### Final main Static Validated — **PASS**

Merge commit `35e38aa812cdb2219e4a4c19e3dcd14b18de0692` に対する `Validate VReview` run #44 が成功した。

- JavaScript / MJS syntax: success
- Project structure / references / Guide / Diagnostics validation: success
- Storage regression tests: success

途中PRの成功だけを最終確認として流用していない。

### GitHub Pages Build / Deploy — **PASS**

同じMerge commit `35e38aa812cdb2219e4a4c19e3dcd14b18de0692` に対するPages run #162で:

- Jekyll build: success
- Pages artifact upload: success
- GitHub Pages deploy: success

### Browser Validated — **Not verified**

Firefox / ChromiumでDiagnostics・Media・Layoutの実操作は未確認。

### Real Media Validated — **Not verified**

実MP4 / WebMでDetector / Feedback v5を最後まで通す確認は今回未実施。

### User Validated — **Not verified**

v0.6.0をユーザーが実際に操作した結果は未確認。

---

## Known Issues / 未完了

- Detector v0.5.0の未知クリップ汎化性能
- ace4-1型の重複Scene
- 長い連キルの自動分割
- Death専用検出
- HUD Scale / Aspect Ratio差
- 固定ROI自動キャリブレーション
- 長尺動画Performance
- Timeline Start / End直接ドラッグ
- ChatGPT採点用30 / 60fps Package
- result JSON Import / History / Training
- Diagnosticsの実Firefox / Chromium検証

---

## 2026-08-30 Web Project Guide v1.1.0 準拠改修

### Project Profile

- VReview: **v0.5.1**
- Detector: **v0.5.0**
- Feedback Package: **v5**
- Storage Schema: **v1**
- Feedback Handoff Schema: **v1**
- Web Project Guide: **v1.1.0**
- Profiles: **STATIC + MEDIA + AI-HANDOFF + TOOL**

正本:

- Runtime Version: `js/version.js`
- Project Metadata: `project-meta.json`
- Current Specification: `SPEC.md`
- Current Overview: `README.md`

Detectorの閾値・Scene判定条件は今回変更していない。Guide準拠の保存・Import Validation・UI・保守・検証基盤を修正したため、Detector精度比較では引き続きDetector v0.5.0として扱う。

---

## 確認したGuide

`EliteMay/web-project-guide` の最新GitHub状態を確認し、`README.md` / `START_HERE.md` から既存Webツール改善ルートを選択した。

主な参照:

- Governance
- Architecture
- Data / Storage
- UI / UX / Accessibility
- Performance / Reliability
- Security
- Testing / Quality
- GitHub Pages
- Version / Maintenance
- Project Management
- Project Profiles
- Dependencies / Assets
- Failure Catalog
- Quality Checklist

GuideのFailure CatalogでVReview自身の過去失敗として記録されているVersioned Patch、Single Source of Truth不在、未実装機能表示、state破損、Object URL cleanup不足、Static Checkのみでの確認不足も再確認した。

---

## 今回の変更

### Source of Truth

新規:

- `project-meta.json`
- `SPEC.md`

Guide Version / Profiles / Storage Schema / AI Handoff Schema / GitHub Pages base pathを明示した。

READMEは現在の利用方法中心へ整理し、詳細仕様は`SPEC.md`、変更とVerification Stateは本ファイルへ分離した。

### Storage Schema / Compatibility

`js/storage.js`へSchema Version付き保存を追加。

新規保存形式:

```text
vreview-storage
├─ schemaVersion
├─ revision
├─ updatedAt
└─ data
```

v0.5.0以前のplain Array / Object形式も読み込めるため、既存Draftを強制削除しない。

破損JSONは可能ならRecovery keyへrawを退避し、元データを即上書きしない。

### Draft Backup / Restore

既存Draftから新規開始する場合:

```text
既存Draft
↓
Backup
↓
新規開始
```

main Draftがない状態でBackupが残っていれば、同じ動画選択時に復元できる。

### Destructive Action

Scene削除へ`元に戻す`を追加。

### Storage Failure / Multi-tab

- localStorage read / write / schema / migration / backup失敗をEvent化
- New Review上で保存失敗を表示
- 同一動画Draftが別タブで更新された場合、競合警告を表示

完全な自動Mergeは未実装。

### AI-HANDOFF Import Validation

新規:

- `data/detector-feedback-schema.json`

Detector Testで以下をImport前Validationする。

- Package schema / Version
- 必須JSON
- ZIP size / entry数 / JSON size
- Scene数
- Start / End
- feedback label
- review tier
- source
- confidence
- ZIP内Path
- JSON構文

既存Feedback v4 / v5を対応対象とする。

ZIP解析では集計不要のJPEG等をMemoryへ保持せず、必要JSONだけ読むよう変更した。

### DOM Safety

Import由来Clip名等は`textContent`中心で描画。

Video metadataもHTML文字列連結をやめ、DOM + `textContent`へ変更。

### UI State

New Review:

- Loading
- Video Error
- Storage Error
- Draft Restore
- Backup Restore
- Detector Error + 手動復帰
- Feedback ZIP Error

Detector Test:

- Empty
- Loading / Validation
- Error
- Success

を明示した。

### Accessibility / Layout

- `:focus-visible`
- `prefers-reduced-motion`
- Dropzone Enter / Space操作
- `aria-live`
- `aria-current`
- Progressのlabel
- 操作対象サイズ調整
- 520px以下Navigation 1列化
- 低い縦解像度用PC Layout

常時Version表示は右下fixed overlayを廃止し、Sidebar内へ移動した。

PC版New Reviewの要求:

```text
左Navigation固定
中央動画固定
右Scene Paneのみ縦Scroll
```

は維持。

### Version / Cache

`js/version.js`へ:

- Storage Schema
- Feedback Schema
- Guide Version

を追加。

CSS / JS Cache BustingはBuild `20260830-2`へ統一した。

### Static Validation

`scripts/validate.mjs`を拡張。

確認対象:

- 必須ファイル
- JSON構文
- Guide / Profile metadata
- Storage / Feedback Schema整合
- HTML local references
- Cache Versionずれ
- duplicate HTML ID
- legacy Detector再混入
- Runtime Versionずれ
- localhost / PC固有Path
- 代表的Secret Token
- public JSON Data URL

### Regression Test

新規:

- `tests/storage.test.mjs`

確認:

- legacy Draft読込
- Schema v1保存 / 読込
- revision更新
- Backup copy
- corrupt JSON Recovery
- write failure通知

### Browser Checklist

新規:

- `tests/BROWSER_CHECKLIST.md`

Firefox / Chromium、100 / 125 / 150% Zoom、低い縦解像度、Media Codec、固定Pane、Draft / Backup、Feedback ZIP等を対象とする。

Checklistを作っただけではBrowser Validated扱いにしない。

---

## Compatibility

### 維持

- GitHub Pages URL
- `index.html`
- `review.html`
- `detector-test.html`
- Detector v0.5.0判定ロジック
- Feedback Package v5出力
- Detector Test v4 / v5 Import
- v0.5.0以前のScene Draft / meta読込

### 変更

新しく保存するDraft / meta / last detector summaryはStorage Schema v1 envelope形式。

旧形式を読み込めるため、Migration目的の一括削除はしていない。

---

## Verification State

### Implemented

- Guide metadata / SPEC
- Versioned Storage
- Legacy Storage compatibility
- Draft Backup / Restore
- Scene delete Undo
- Storage failure UI
- Cross-tab warning
- Feedback Import Validation
- Accessibility改善
- Static Validator拡張
- Storage Regression Test
- Browser manual checklist

### Static Validated — **PASS**

GitHub Actions `Validate VReview` で以下が成功:

- JavaScript / MJS syntax
- Project structure / references / JSON / metadata validation
- Storage regression tests

最新Guide改修コミットに対するValidate workflowは成功確認済み。

### GitHub Pages Build / Deploy — **PASS**

同じ最新Guide改修コミットに対して:

- Jekyll build: success
- Pages artifact: success
- GitHub Pages deploy: success

### Browser Validated — **Not verified**

実ブラウザでの主要導線・Geometry・Zoom確認は未実施。

### Real Media Validated — **Not verified**

実MP4 / WebMでDetector / Feedback Package v5を最後まで通す確認は未実施。

### User Validated — **Not verified**

今回のv0.5.1変更後の実利用確認は未実施。

---

## Known Issues / 未完了

- Detector v0.5.0の未知クリップ汎化性能
- ace4-1型の重複Scene
- 長い連キルの自動分割
- Death専用検出
- HUD Scale / Aspect Ratio差
- 固定ROI自動キャリブレーション
- 長尺動画Audio解析中のUI responsivenessは改善余地あり
- Timeline Start / End直接ドラッグ
- ChatGPT採点用30 / 60fps Package
- result JSON Import / History / Training

---

## 前段 v0.5.0 基盤改修 — 要約

v0.4系で積み重なっていたVersioned Detector Patchを`js/detector.js`の単一Pipelineへ統合し、旧Runtimeを本番フォルダから削除した。

同時に:

- validate-before-commitによるScene state修正
- Object URL cleanup
- 動画Fingerprint Draft復元
- Detector Cancel
- 固定動画 + 右Pane Scroll
- weak候補UI
- Feedback Package v5
- Detector Test
- Version一元化
- 初期GitHub Actions validation

を実装した。

詳細な過去差分はGit履歴、現在仕様は`SPEC.md`を正本とする。
