# 作業報告書

## 2026-08-29 初期構築〜検出改善フロー追加

### 今回変更した内容

- VReviewプロジェクトを初期化
- READMEへ要件定義と崩してはいけない仕様を整理
- GitHub Pages前提の静的Web構成を採用
- ChatGPT Plusへ手動で解析パッケージを渡す方式を正式仕様として記載
- Dashboard / New Review / Import Result / History / Training / Settings のページ土台を追加
- MP4 / WebMのブラウザ内読み込み処理を追加
- 動画時間・解像度・ファイルサイズ表示を追加
- Combat Sceneの手動追加・開始終了調整・削除UIを追加
- Scene範囲をタイムライン上へ表示
- 初期Combat Scene自動検出を実装
- 音声ピーク + 画面変化スコアを組み合わせた候補検出を実装
- SceneごとのConfidence表示を追加
- 自動検出精度改善用のフィードバックZIP生成を追加
- 自動検出直後Sceneとユーザー修正後Sceneを別データとしてZIPへ保存
- 自動検出Sceneと修正後Sceneの16コマ確認画像を自動生成
- ZIP生成を外部JSZip CDN依存から、VReview内蔵のStore方式ZIP生成へ変更
- 古いJSがブラウザキャッシュから読み込まれる問題への対策として `feedback-package-v2.js` を新規追加
- `review.html` のCSS/JS参照にバージョンパラメータを追加し、BUILD 0.3.1としてキャッシュを更新
- 確認画像生成が失敗した場合でもJSON中心の提出ZIPを生成できるフォールバックを追加

### 変更したファイル

- README.md
- 作業報告書.md
- review.html
- css/components.css
- js/app.js
- js/scene-detection.js
- js/feedback-package.js
- js/feedback-package-v2.js（新規）
- js/ui.js
- その他初期ページ / CSS / JS

### 修正した不具合

- Scene数を12件に制限していた初期実装を修正し、1クリップ内の全検出Sceneを保持するよう変更
- 動画0秒付近の映像解析でフレーム準備前に処理する可能性を減らすため、映像解析開始時の待機条件を `loadeddata` に変更
- 検出改善用ZIPが外部JSZip CDNの読み込みに依存していた問題を修正
- GitHub Pages / ブラウザ環境でCDNが失敗してもZIP生成部分自体が動けるよう、外部依存を削除
- 修正版反映後もブラウザが旧 `feedback-package.js` をキャッシュし「ZIPライブラリを読み込めませんでした」と表示する問題に対して、ファイル名変更とバージョン付き参照でキャッシュ回避

### 追加した機能

#### Combat Scene自動検出

- 音声RMS / Peak / Rise / Crestを利用した音声ピークスコア
- 低解像度フレーム差分による画面変化スコア
- 中央領域の画面変化を別スコア化
- 音声 + 映像の複合スコアでCombat候補イベント生成
- 近い候補イベントをSceneへまとめる
- 0.7秒前 / 0.8秒後へScene範囲を拡張
- 長すぎるSceneの簡易分割
- HIGH / MEDIUM / LOW Confidence表示

#### 検出改善用ZIP

自動検出後にユーザーが誤検出削除・範囲修正・見逃しScene追加を行い、以下をZIPへ出力する。

- manifest.json
- auto-scenes.json
- corrected-scenes.json
- detector-diagnostics.json
- notes.txt
- auto-scenes/*.jpg
- corrected-scenes/*.jpg

元動画はZIPに含めない。

ZIPは圧縮率より互換性と外部依存排除を優先し、ブラウザ内でStore方式ZIPとして生成する。確認画像生成がブラウザ依存で失敗した場合は、エラー内容を `image-generation-warning.txt` に入れ、JSON中心のZIPを生成する。

### 現在の課題

- 自動検出精度はまだ実用レベルに達していない
- 銃声以外の大きな音や激しい視点移動をCombat Sceneと誤認する可能性が高い
- キルフィード / デスUI / 射撃フラッシュなどVALORANT固有情報はまだ検出に使っていない
- 現状の映像解析間隔は0.25〜0.30秒のため、高速な戦闘開始点の特定には粗い
- 自動Sceneと修正後Sceneの対応付けはまだID単位ではなく時間比較が中心になる

### 未完了の項目

- 実クリップのフィードバックZIPを使った自動検出精度改善
- Scene結合 / 分割の本UI
- 自動30 / 60fps判定
- 採点用フレーム抽出
- Overview / Detailコンタクトシート生成
- ChatGPT採点用解析ZIP
- prompt自動生成
- result JSON解析 / Schema検証
- Review Result本画面
- IndexedDBによる履歴保存
- Training集計
- Settingsフォーム

### 確認できたこと

- GitHub上へ必要ファイルを作成・更新できること
- 自動検出モジュールと提出用ZIP生成モジュールを既存Scene編集UIから分離した構成にできていること
- 検出改善用ZIPが外部CDNを必須としない構成へ変更されたこと
- GitHubコード検索上、旧エラー文言 `ZIPライブラリを読み込めませんでした` が現行コードから消えていること

### 確認できていないこと

- GitHub Pages実ブラウザ上でのBUILD 0.3.1のZIP生成動作
- Firefox / Chromeでの動画シーク差
- 実VALORANTクリップでの提出用コンタクトシート品質
- 大量Scene時のZIP生成時間 / メモリ使用量
- detector-diagnostics.jsonの実データ量

### 今後必要な作業

1. BUILD 0.3.1のページを開いて提出ZIP生成を実ブラウザ確認
2. 実VALORANTクリップで自動検出
3. ユーザーがSceneを正しい状態へ修正
4. `検出改善用ZIPを作成`
5. 生成ZIPをChatGPTへ渡して誤検出 / 見逃し / 範囲ズレを分析
6. scene-detection.jsの特徴量・閾値・VALORANT固有UI検出を改善
7. 複数クリップで同じ流れを繰り返して精度を上げる
8. 精度が安定した後に採点用30 / 60fpsコンタクトシートとChatGPT採点ZIPへ進む
