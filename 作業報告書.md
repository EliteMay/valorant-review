# 作業報告書

## 2026-08-29 Detector v0.4.3

### 現在バージョン

- VReview: **v0.4.3**
- New Review: **BUILD 0.4.3**
- Base Detector: **v0.4.2**
- Scene Refiner: **v0.4.3**
- Detector Feedback Package: **v3**

## v0.4.3 実動画再検証

提出ファイル:

- `vreview_feedback_VALORANT_08-28-2026_15-35-23-034(1).zip`
- 動画長: **44.167秒**
- 解像度: **1920x1080**
- 検出感度: **standard**
- Detector: **v0.4.3**
- ユーザーメモ: **文句なし**

### 自動検出結果

1. **13.30 - 20.10秒**
   - `kill`
   - Confidence 96%
   - detector_reason: `kill-confirm+v043`
   - anchor_count: 45
   - shot_evidence_count: 2
   - 前版の `0.00 - 20.00秒` から大幅に短縮
   - 実際の交戦前後を含む範囲に収まった

2. **28.50 - 32.80秒**
   - `kill`
   - Confidence 96%
   - detector_reason: `killfeed-with-shots+v043`
   - anchor_count: 3
   - shot_evidence_count: 1
   - 前側のPre-roll追加後も不要部分が過剰に増えていない

3. **38.17 - 44.167秒**
   - `kill`
   - Confidence 98%
   - detector_reason: `kill-confirm+v043`
   - anchor_count: 10
   - shot_evidence_count: 2
   - 前版で不足していたキル前時間を確保

### Refiner結果

`detector-diagnostics.json` の `refiner`:

```text
Dropped:
- 23.05 - 26.95秒
- reason: no-shot-and-no-local-kill-confirmation

Adjusted:
- 0.00 - 20.00 -> 13.30 - 20.10
- 29.40 - 32.80 -> 28.50 - 32.80
- 39.07 - 44.167 -> 38.17 - 44.167
```

前回 `false_positive` とされたSceneを自動で除外し、3つの正解Sceneだけが残った。

### 正解ラベル集計

- `kill`: **3**
- `false_positive`: **0**
- 手動追加: **0**
- 時間手動修正: **0**

今回の44秒クリップでは、**自動検出だけでユーザーが修正不要と判断する結果に到達**した。

### 画像確認

3Sceneの確認用コンタクトシートも確認。

- Scene 1: 接敵前の準備からキル後までを含む
- Scene 2: Peek前から射撃・キル確認までを含む
- Scene 3: キル前の情報量が前版より増え、レビュー用Pre-rollとして改善

Scene 3はクリップ末尾がラウンド終了と重なるため終了後UIも含むが、ユーザー評価は「文句なし」であり、このサンプルでは追加修正対象としない。

## v0.4.3で解決できた問題

前回の同一クリップで発生していた以下3点をすべて改善できた。

1. **長すぎるScene**
   - 0〜20秒 -> 13.3〜20.1秒
2. **不要な誤検出Scene**
   - 23.05〜26.95秒 -> 自動除外
3. **キル前時間不足**
   - 39.07秒開始 -> 38.17秒開始

## 今後の方針

この1クリップに対してさらに閾値を調整すると過学習する可能性があるため、**Detector v0.4.3の条件は一旦固定**する。

次は別クリップを複数本使い、以下を確認する。

- 別マップ
- 別エージェント
- 別武器
- 1タップ / スプレー
- 連続キル
- デスで終わる場面
- アビリティ使用を多く含む場面
- ラウンド終了直前のキル

別クリップでも修正不要または軽微な修正だけで安定することを確認できた段階で、自動検出をv1用として固定し、**30 / 60fpsのChatGPT採点用パッケージ実装へ進む**。

## v0.4.3 実装概要

### 1. v0.4.2 + Refiner構成

映像・音声特徴抽出はv0.4.2を維持する。

`review.html` では以下の順に読み込む。

1. `scene-detection-v042.js`
2. `scene-detection-v043.js`

v0.4.3はv0.4.2の結果を補正するレイヤーとして実装。

### 2. 長すぎるSceneのFocus Window

Standardでは8.8秒を超えるSceneを再評価。

0.1秒刻みで約5.8秒Windowを動かし、

- shot-hud
- combat-support
- killfeed-support
- audio
- center motion

が密な区間を選ぶ。

### 3. False Positive Gate

特に `detector_reason = kill-confirm` のSceneで、

- shot_evidence_count = 0
- Kill-confirm時点のKillfeedが弱い
- Ammo / Audioによる本人キル根拠も弱い

場合は除外する。

### 4. Pre-roll追加

短いSceneの開始位置を追加で前へ伸ばす。

- Low: **0.65秒**
- Standard: **0.90秒**
- High: **1.10秒**

### 5. Refiner診断

`detector-diagnostics.json` に以下を保存。

```text
refiner.dropped
refiner.adjusted
```

## 変更したファイル

今回の再検証ではDetectorコード自体は変更していない。

- `作業報告書.md` の検証結果のみ更新

## 確認できたこと

- GitHub Pages上でv0.4.3を実クリップへ再実行したFeedback ZIPを受領
- 3Sceneすべて `kill`
- 誤検出0
- 手動追加0
- 手動時間修正0
- `refiner.dropped` が前回の誤検出Sceneを正しく削除
- `refiner.adjusted` が前回の3つの範囲問題を狙い通り補正
- 確認画像でも3Sceneがレビュー対象として妥当
- ユーザー評価「文句なし」

## まだ確認できていないこと

- v0.4.3の別クリップでの汎化性能
- Death Scene専用検出
- HUDスケール差
- Firefox / Chrome差
- 長尺動画での処理時間 / メモリ使用量
- 30 / 60fps採点用パッケージ

## 次に必要な作業

1. **v0.4.3のDetector条件は一旦変更しない**
2. 別クリップを2〜3本以上で検証
3. 各Sceneへ正解ラベルを設定してFeedback ZIPを提出
4. 別条件でも誤検出/見逃しが少ないか確認
5. 必要な場合のみv0.4.4へ調整
6. 安定したらChatGPT採点用30 / 60fpsパッケージを実装
