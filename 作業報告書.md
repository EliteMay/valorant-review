# 作業報告書

## 2026-08-29 VReview v0.4.5 UI改善

### 現在バージョン

- VReview: **v0.4.5**
- New Review: **BUILD 0.4.5**
- Base Detector: **v0.4.2**
- Scene Refiner: **v0.4.3**
- Recall Guard / Detector: **v0.4.4**
- Detector Feedback Package: **v3**

### ユーザー指摘

New Reviewでページ全体が縦スクロールし、動画まで画面外へ流れるためScene確認時に使いにくかった。

要望は、

```text
左ナビ: 固定
中央動画: 固定
一番右のScene操作領域: スクロール
```

というPC向けレイアウト。

### 今回変更した内容

- PC版でアプリ全体を100vh内へ固定
- 左サイドバーを固定領域化
- 動画読み込み後はNew Reviewのメイン領域を画面内へ固定
- 中央の動画 / Timeline / Scene追加操作をスクロールさせない
- 一番右の自動検出 / Scene一覧 / Feedback / AI Package領域だけ `overflow-y: auto` に変更
- 動画読み込み後は大きなアップロードDropzoneを非表示
- 中央上部へ `動画を変更` ボタンを追加
- 自動検出完了時に右Sceneペインだけ先頭へ戻す
- 980px以下では固定レイアウトを解除し、縦並びの通常スクロールへ戻す
- 右Sceneペインに独立スクロールバーを表示
- サイトバージョンをv0.4.5へ更新
- Detectorロジックはv0.4.4から変更していない

### 変更ファイル

- `css/layout.css`
- `css/components.css`
- `css/base.css`
- `review.html`
- `js/app.js`
- `index.html`
- `result.html`
- `history.html`
- `training.html`
- `settings.html`
- `README.md`
- `作業報告書.md`

### 確認できたこと

- HTML / CSS構成上、PC版では右Sceneペインのみが独立スクロール対象になるよう変更した
- 動画読み込み時に `review-loaded` classを付与する処理を追加した
- `動画を変更` ボタンから既存のfile inputを再利用できる構成にした
- モバイル幅では固定スクロールを解除するCSSを追加した
- Detectorコード自体には変更を加えていない

### 確認できていないこと

- GitHub Pages実ブラウザ上でのスクロール感
- 低い縦解像度のディスプレイで動画操作ボタンまで常時収まるか
- Firefox / Chromeでのスクロールバー表示差
- 980px境界付近の実画面レイアウト

---

## 2026-08-29 Detector v0.4.4

### v0.4.4検証前の状態

- VReview: v0.4.4
- New Review: BUILD 0.4.4
- Base Detector: v0.4.2
- Scene Refiner: v0.4.3
- Recall Guard: v0.4.4
- Detector Feedback Package: v3

### 今回まとめて確認した4クリップ

#### ace2.mp4

- 48.017秒 / 1724x1080
- v0.4.3で3キル目・4キル目が抜けた
- Base Detectorの `11.85 - 26.65` をFocus Windowが `14.35 - 21.15` へ縮め、後半の強いShot / Kill-confirm群を切っていた

#### ace.mp4

- 42.634秒 / 1152x720
- v0.4.3で3キル目を1つ見逃し
- `29.10 - 35.05` のBase候補がHard Dropされていた

#### ace3.mp4

- 31.717秒 / 1724x1080
- 2Sceneともkill
- 連キル判定もユーザー評価「完璧」

#### ace4-1.mp4

- 25.167秒 / 1724x1080
- Scene 1はfalse_positive
- Scene 2はkill
- 連キル境界で重複気味

### v0.4.4実装方針

処理順:

```text
v0.4.2 Base Detector
↓
v0.4.3 Refiner
↓
v0.4.4 Recall Guard
```

#### Hard DropをLOW候補として復元

v0.4.3 `refiner.dropped` に入った候補について、周辺にイベント証拠が残っていれば完全削除せず、

- Confidence約42〜59%
- `recovered-low-confidence`
- `needsReview`

として一覧へ戻す。

#### 長い連キルの後半復元

Focus Window後ろ側に、

- 1.35秒以内にshot-hud 3件以上
- Audio + Killfeed + Ammoが同時に強いKill-confirm
- 複数Kill-confirmとShot / Killfeedの組み合わせ

が残る場合は元Scene終端側を復元する。

#### 診断

```text
recallGuard.recovered
recallGuard.expanded
```

を保存。

#### 重複Scene

短い方の72%以上が重複する候補のみ統合し、強すぎる統合は避ける。

### v0.4.4で予想されるトレードオフ

改善見込み:

- 4kで消えた弱候補がLOWとして再表示
- aceで消えた候補がLOWとして再表示
- ace2でFocus Window後半の連キルを復元

意図的に許容:

- 以前削除できた誤検出候補がLOWで戻る可能性

不要LOW Sceneが増えることより、本物のキルが消えることを重大と判断してRecallを優先した。

### v0.4.4で変更したファイル

- `js/scene-detection-v044.js`
- `review.html`
- `css/base.css`
- `index.html`
- `result.html`
- `history.html`
- `training.html`
- `settings.html`
- `README.md`
- `作業報告書.md`

### v0.4.4でまだ確認できていないこと

- 今回の複数クリップへの実ブラウザ再実行結果
- LOW復元候補のPrecision
- ace2の後半キル復元
- ace4-1の重複問題の再現頻度
- Death専用検出
- HUDスケール差
- Firefox / Chrome差
- 長尺動画性能

### 次のDetector検証

1. Detector v0.4.4で既存クリップを再検出
2. LOW候補もすべてラベル付け
3. Feedback ZIPを複数まとめて提出
4. LOW候補のPrecision / Recallを集計
5. 必要ならDetector v0.4.5以降で誤検出を減らす
6. 検出安定後に30 / 60fps ChatGPT採点パッケージへ進む
