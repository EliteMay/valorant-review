# 作業報告書

## 2026-08-30 VReview v0.5.0 基盤改修

### 現在バージョン

- VReview: **v0.5.0**
- Detector: **v0.5.0**
- Detector Feedback Package: **v5**

バージョン表示元は `js/version.js` に一元化した。

---

## 今回の目的

既存VReviewを「Detectorの実験コードが継ぎ足された開発途中サイト」として厳しく見直し、Detector精度調整を続ける前に、操作性・保守性・保存・エラー耐性・検証方法の基盤を修正した。

今回の優先項目:

1. Detector継ぎ足し構造の解消
2. Scene state破損バグ修正
3. 動画Blob URLの解放
4. 本当の途中保存 / 復元
5. 未実装ページを通常機能から分離
6. Feedback Packageの改善
7. Detector検証結果を定量化するページ追加
8. GitHub Actionsで構文 / 参照切れを自動検証

---

## 変更した内容

### 1. Detectorを単一Pipelineへ統合

旧構成:

```text
scene-detection-v042.js
↓ global上書き
scene-detection-v043.js
↓ global上書き
scene-detection-v044.js
↓ global上書き
scene-detection-v045.js
```

問題:

- どの版が最終責任を持つか分かりにくい
- ある版で削除したSceneを次版で戻すなど、修正同士が打ち消し合う
- 読み込み順依存
- 古いJSを残す必要がある

v0.5.0:

`js/detector.js` の1ファイルへ統合。

内部処理:

```text
Audio / Visual Analysis
↓
Evidence Builder
↓
Base Scene Builder
↓
Scene Refiner
↓
Recall Guard
↓
Candidate Classifier
```

旧Detector JSはGit履歴に残るため本番 `js/` から削除した。

### 2. 解析キャンセル

Detectorへ `AbortSignal` 対応を追加。

New Reviewへ `解析をキャンセル` ボタンを追加し、Visual seekループとAudio解析ループの途中でキャンセル可能にした。

### 3. 長尺動画の安全策

- 120秒超では警告
- Audio解析stepを少し広げる
- Visual解析stepを最大0.30秒へ広げる

長尺動画の本格的な粗解析→詳細解析の二段方式は未実装。

### 4. Scene state破損バグ修正

旧 `updateScene()` は元Sceneへpatchを直接書き込んでから検証していた。

Start >= Endなど不正入力時、検証失敗しても元stateだけ変更済みになる可能性があった。

v0.5.0では:

```text
sceneコピー
↓
patch適用
↓
normalize / validate
↓
正常な場合のみstateへ反映
```

へ変更。

### 5. escapeHtml修正

旧 `ui.js` の文字エスケープ処理に誤ったキーがあり、一部文字列で `undefined` が混ざる可能性があった。

通常の `& < > " '` を正しくエスケープする処理へ修正。

### 6. 動画Object URLリーク修正

`video.js` を更新し、同一video elementへ新しい動画を読み込む前に前回の `URL.createObjectURL()` を `URL.revokeObjectURL()` する。

ページ離脱時も現在動画URLを解放する。

### 7. 動画ごとの途中保存

動画情報からFingerprintを作成:

- file name
- size
- lastModified
- duration
- resolution

保存:

- Scene一覧
- feedback label
- review tier
- sensitivity
- feedback notes

同じ動画を再選択すると前回Sceneを復元するか確認する。

Detector diagnosticsはlocalStorage容量を圧迫するため保存しない。Feedback ZIPを再生成する場合は自動検出を再実行する。

### 8. New Review操作改善

維持:

```text
左ナビ: 固定
中央動画: 固定
右Sceneペイン: のみスクロール
```

追加:

- Timelineクリックでシーク
- 現在位置Playhead
- 選択Scene強調
- weak折りたたみopen状態維持
- Space: 再生 / 停止
- I: Scene開始候補
- O: Scene終了候補
- ← / →: ±0.1秒
- Shift + ← / →: ±0.5秒
- Delete: 選択Scene削除

Timeline上でStart / Endハンドルを直接ドラッグする機能は未実装。

### 9. 未実装ページを通常ナビから分離

以下はまだ本機能未実装:

- Import / Result
- History
- Training
- Settings

従来は通常ナビに並び、完成済みページのように見えた。

v0.5.0では通常ナビを:

- Dashboard
- New Review
- Detector Test

へ絞った。

未実装ページは直接アクセス可能だが `DEVELOPMENT` と明示し、通常ナビから外した。

### 10. Dashboard再設計

未実装AI採点値の `AIM AVG / MOVEMENT AVG` ダミー表示を削除。

現在実際に利用できる情報へ変更:

- Detector Version
- 直近Primary Scene数
- 直近Weak候補数
- 直近Clip名
- v0.5.0実装状況

### 11. Feedback Package v5

旧問題:

- auto / correctedで同一Scene画像を二重生成
- `cover`描画でHUDが切れる可能性
- Scene全体から均等16枚なので、0.1秒程度の重要Eventを逃しやすい

v5:

- Scene範囲をdedupeし共有画像化
- `scene-image-map.json`追加
- 全画面画像は`contain`描画
- Kill Confirm / Shot HUD / Killfeed / Combat SupportのEvent時刻を優先
- Killfeed / Ammo / Kill Confirm / Round UIのROI拡大画像を追加

構成:

```text
vreview_feedback_<clip>.zip
├─ README.txt
├─ manifest.json
├─ auto-scenes.json
├─ corrected-scenes.json
├─ detector-diagnostics.json
├─ scene-image-map.json
├─ notes.txt
└─ scene-images/
   ├─ scene_001_full.jpg
   ├─ scene_001_roi.jpg
   └─ ...
```

### 12. Detector Test追加

`detector-test.html` を追加。

複数Feedback ZIPをまとめて読み込み:

- Precision
- Recall
- primary Precision
- TP / FP / FN
- weakへ落ちた有効Scene
- 未確認ラベル
- Detector Version別集計

を自動計算する。

これは元動画なしでDetector自体を再実行する完全なRegression Testではなく、提出結果の定量比較ツール。

### 13. バージョン一元化

`js/version.js`:

```text
app: 0.5.0
detector: 0.5.0
feedback: 5
```

サイト内表示はこの情報を利用する。

CSS右下バージョンも `body[data-vreview-version]` から表示する。

### 14. GitHub Actions検証

追加:

- `.github/workflows/validate.yml`
- `scripts/validate.mjs`

push / pull request時:

- JS / MJS `node --check`
- HTMLのローカルsrc / href参照確認
- `review.html` が `js/detector.js` を使用しているか
- Feedback Package v5を使用しているか
- 旧versioned Detectorを読み込んでいないか

を確認する。

---

## 削除した旧ファイル

Git履歴に残るため、本番フォルダから削除:

- `js/scene-detection.js`
- `js/scene-detection-v040.js`
- `js/scene-detection-v041.js`
- `js/scene-detection-v042.js`
- `js/scene-detection-v043.js`
- `js/scene-detection-v044.js`
- `js/scene-detection-v045.js`
- `js/feedback-package.js`
- `js/feedback-package-v2.js`
- `js/feedback-package-v3.js`
- `js/feedback-package-v4.js`

---

## 主な変更ファイル

### 新規

- `js/version.js`
- `js/detector.js`
- `js/feedback-package-v5.js`
- `js/detector-test.js`
- `detector-test.html`
- `scripts/validate.mjs`
- `.github/workflows/validate.yml`

### 更新

- `index.html`
- `review.html`
- `result.html`
- `history.html`
- `training.html`
- `settings.html`
- `js/app.js`
- `js/video.js`
- `js/ui.js`
- `css/base.css`
- `css/components.css`
- `README.md`
- `作業報告書.md`

---

## 修正した不具合

- Scene時間不正入力時に内部stateだけ壊れる可能性
- `escapeHtml()`の誤った置換
- 動画切替時のBlob URL解放不足
- SceneをlocalStorageへ書くだけで復元していなかった問題
- weak候補のラベル変更後に折りたたみ状態が戻る問題
- ページごとに表示バージョンがずれる構造
- 未実装ページが通常機能に見える問題
- Feedback画像でHUDがクロップされる問題
- auto / corrected画像の重複生成

---

## 追加した機能

- Detector単一Pipeline
- Detector解析キャンセル
- 動画FingerprintによるScene復元
- Timeline Playhead
- Timelineクリックシーク
- キーボード操作
- Detector Test
- Feedback Package v5
- ROI確認画像
- GitHub Actions自動検証

---

## 未完了

- 未使用クリップでDetector v0.5.0再検証
- ace4-1型重複Sceneの一般化された修正
- 長い連キルSceneの自動分割
- Death専用検出
- HUD Scale / Aspect Ratio差への対応
- 固定ROI自動キャリブレーション
- 長尺動画の粗解析→詳細解析
- Timeline Start / Endドラッグハンドル
- Auto 30 / 60fps
- ChatGPT採点用高fpsPackage
- result JSON検証 / 表示
- History
- Training
- Settings

---

## 確認できたこと

- GitHub上のHTML / JS / CSS参照を現行v0.5.0構成へ切替
- review.htmlから旧versioned Detector参照を除去
- 旧Detector / Feedback JSを本番フォルダから削除
- GitHub Actionsによる構文 / 参照チェックの仕組みを追加
- v0.5.0の主要コードをGitHubへ反映

## 確認できていないこと

- GitHub Pages実画面でNew Reviewの全操作を通したE2E確認
- Firefox / Chrome双方での解析キャンセル動作
- 実動画でDetector v0.5.0がv0.4.5と同等以上の検出精度を維持できるか
- Feedback Package v5で実ZIPを最後まで生成できるか
- Detector Testで実際のv5 ZIP複数件を読み込んだ結果
- 低い縦解像度で固定動画エリアが常に使いやすいか

これらは未確認のまま完成扱いにしない。

---

## 次に必要な作業

1. GitHub Actionsの最新Validate結果を確認
2. GitHub Pagesでv0.5.0を開く
3. 既知クリップを1〜2本だけ再テストし、v0.4.5から重大な回帰がないか確認
4. 未使用クリップを複数本でDetector v0.5.0検証
5. Feedback Package v5を提出し、ROI画像とEvent優先画像の有効性確認
6. Detector Testで複数ZIPを集計
7. 共通失敗が十分集まってからDetectorを次版へ更新
