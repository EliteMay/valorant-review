# 作業報告書

## 2026-08-29 初期構築〜Detector v0.4.0

### 現在バージョン

- VReview: **v0.4.0**
- New Review画面: **BUILD 0.4.0**

### 今回変更した内容

- VReviewプロジェクトを初期化
- READMEへ要件定義と崩してはいけない仕様を整理
- GitHub Pages前提の静的Web構成を採用
- ChatGPT Plusへ手動で解析パッケージを渡す方式を正式仕様として記載
- Dashboard / New Review / Import Result / History / Training / Settings のページ土台を追加
- MP4 / WebMのブラウザ内読み込み処理を追加
- 動画時間・解像度・ファイルサイズ表示を追加
- Combat Sceneの手動追加・開始終了調整・削除UIを追加
- Scene範囲をタイムライン上へ表示
- 初期Combat Scene自動検出を実装
- SceneごとのConfidence表示を追加
- 自動検出精度改善用のフィードバックZIP生成を追加
- 自動検出直後Sceneとユーザー修正後Sceneを別データとしてZIPへ保存
- 自動検出Sceneと修正後Sceneの16コマ確認画像を自動生成
- ZIP生成を外部JSZip CDN依存から、VReview内蔵のStore方式ZIP生成へ変更
- 古いJSキャッシュ対策として `feedback-package-v2.js` とバージョン付きURLを導入
- サイト全体に `VReview v0.4.0` の常時表示を追加
- 最初の実VALORANTフィードバックZIPを解析し、Detector v0.4.0を追加

## 最初の実クリップ検証

提出ファイル:

- `vreview_feedback_VALORANT_08-25-2026_15-35-47-221.zip`
- 動画長: 約23.067秒
- 解像度: 1920x1080
- 検出感度: standard

### 自動検出結果

1. 11.9〜13.4秒
   - ユーザー評価: **完全な誤検出**
   - 確認画像ではPhoenixのアビリティ使用場面
   - 音声スコアが非常に高い一方、映像変化は弱い
   - 旧検出器が音声ピークを信用しすぎていたことを確認

2. 14.0〜19.95秒
   - ユーザー評価: **正しく検出**
   - 確認画像では実際の銃撃戦と複数のキルフィード更新を確認

### 見逃し

ユーザーメモ:

- `三つキルを見逃している`

旧診断データ上で自動Scene外に強い候補が出ていた主な時刻:

- 約2.75秒
- 約4.9秒
- 約10.2〜10.25秒

この3箇所は、旧ロジックでは「映像変化が強いが音声条件が不足」「音声と映像が両方中程度で旧閾値に届かない」ため候補から落ちていた。

### 追加で確認した誤検出候補

- 約20.4〜21秒付近は映像変化が大きいが、確認画像上では戦闘終了後のスコアボード・武器切替付近
- 単純な画面変化だけを強くすると、この種のUI変化を誤検出する危険がある

## Detector v0.4.0 の変更

### 1. 音声単独ピークを弱体化

旧検出ではアビリティ音のような巨大な単発音でもCombat Scene化しやすかった。

v0.4.0では、音声が大きいだけではなく一定以上の映像変化も要求する。

### 2. 映像解析の細分化

- 全画面Motion
- 中央領域Motion
- 右上Killfeed領域Motion

を別々に計算する。

### 3. Killfeed ROI追加

VALORANTの右上キルフィード領域を専用ROIとして解析。

単純なカメラ移動で右上背景が変わった場合と区別するため、

- Killfeed領域の絶対変化量
- 全画面Motionとの差分
- Killfeed領域 / 全画面Motionの比率

を併用する。

OCRや文字認識ではなく、まず軽量な領域変化検出として実装。

### 4. 複合判定条件を変更

Combatイベントは主に以下で作る。

- Killfeed領域の局所的な強い変化
- 音声 + 映像が両方一定以上
- 強い映像変化 + 戦闘らしい音声トランジェント
- 強い音声 + 一定以上の映像変化

### 5. UI変化の誤検出抑制

画面が大きく変わっていても、戦闘らしい音声条件やKillfeed変化が弱い場合はイベント化しにくくした。

これにより、最初の検証データ上では20秒以降のスコアボード付近を旧方式より除外しやすくする。

### 6. 解析間隔

- 35秒以下: 0.20秒
- 35〜75秒: 0.25秒
- 75秒超: 0.30秒

短いクリップでは旧0.25秒より細かく見る。

### 7. 診断データ拡張

今後のフィードバックZIPでは以下も保存できる設計に変更。

- killfeedMotion
- killfeedExcess
- killfeedRatio
- killfeedScore
- event kind

これにより、次回から「何を根拠に拾った/落としたか」をより細かく比較できる。

## 変更したファイル

- README.md
- 作業報告書.md
- review.html
- css/base.css
- css/components.css
- js/app.js
- js/scene-detection.js
- js/scene-detection-v040.js（新規）
- js/feedback-package.js
- js/feedback-package-v2.js
- js/ui.js
- その他初期ページ / CSS / JS

## 修正した不具合

- Scene数を12件に制限していた初期実装を修正し、1クリップ内の全検出Sceneを保持するよう変更
- 動画0秒付近の映像解析でフレーム準備前に処理する可能性を減らすため `loadeddata` 待機へ変更
- 検出改善用ZIPが外部JSZip CDNに依存していた問題を修正
- ブラウザキャッシュにより古いZIP処理が残る問題をファイル名変更 + バージョン付き参照で回避
- 確認画像生成が失敗した場合でもJSON中心の提出ZIPを生成できるフォールバックを追加

## 検出改善用ZIP

自動検出後にユーザーが誤検出削除・範囲修正・見逃しScene追加を行い、以下をZIPへ出力する。

- manifest.json
- auto-scenes.json
- corrected-scenes.json
- detector-diagnostics.json
- notes.txt
- auto-scenes/*.jpg
- corrected-scenes/*.jpg

元動画はZIPに含めない。

ZIPは圧縮率より互換性と外部依存排除を優先し、ブラウザ内でStore方式ZIPとして生成する。

## 現在の課題

- Detector v0.4.0は最初の1クリップに基づく改善であり、実用精度は未確認
- Killfeed ROIは文字認識ではなく領域変化なので、背景やスコアボードによる誤反応の可能性がある
- Death UIの専用検出はまだない
- 射撃フラッシュ、弾数減少、武器状態などのVALORANT固有特徴はまだ使っていない
- 自動Sceneとユーザー修正後Sceneの対応付けはまだ時間比較中心
- 今回の提出ZIPではcorrected-scenesがauto-scenesと同一で、誤検出/見逃しの正解時刻はnotes.txtから補完して分析した

## 未完了の項目

- Detector v0.4.0を同じ動画で再テスト
- 別クリップを使ったv0.4.0検証
- Killfeed ROI閾値調整
- Death UI検出
- Scene結合 / 分割の本UI
- 自動30 / 60fps判定
- 採点用フレーム抽出
- Overview / Detailコンタクトシート生成
- ChatGPT採点用解析ZIP
- prompt自動生成
- result JSON解析 / Schema検証
- Review Result本画面
- IndexedDBによる履歴保存
- Training集計
- Settingsフォーム

## 確認できたこと

- 提出ZIPを実際に生成してChatGPTへ渡せること
- 生成ZIP内のmanifest / auto-scenes / corrected-scenes / diagnostics / notes / JPGを読み取れること
- 自動検出Sceneの確認画像から、誤検出がPhoenixアビリティ場面であることを確認
- 正しく検出したSceneでは銃撃戦とキルフィード更新を確認
- 旧diagnosticsから見逃し候補となる時刻帯を抽出できること
- Detector v0.4.0のJavaScriptはローカルで構文チェック済み
- GitHubへv0.4.0用ファイルを反映済み

## 確認できていないこと

- GitHub Pages実ブラウザ上でDetector v0.4.0が同じ動画を何Scene検出するか
- v0.4.0のKillfeed ROIが2.75秒 / 4.9秒付近を実際に拾えるか
- Firefox / Chrome間の動画シーク・Canvas差
- 複数VALORANT HUDスケールでのKillfeed ROI位置
- 大量Scene時の処理時間 / メモリ使用量

## 今後必要な作業

1. BUILD 0.4.0を開く
2. 今回と同じ動画で自動検出を再実行
3. 自動検出結果を正しいSceneへ修正
4. 特に誤検出は削除し、見逃しは手動追加してから提出ZIPを生成
5. 次のZIPをChatGPTへ渡す
6. v0.4.0の誤検出 / 見逃し / 時間ズレを再分析
7. 複数クリップで同じ流れを繰り返す
8. 検出精度が安定した後に30 / 60fps採点パッケージへ進む
