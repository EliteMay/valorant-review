# 作業報告書

## 2026-08-30 Web Project Guide v1.1.0 準拠改修

### 採用ルール

- Web Project Guide: **v1.1.0**
- Profiles: **STATIC + MEDIA + AI-HANDOFF + TOOL**
- Project metadata: `project-meta.json`
- Current specification: `SPEC.md`
- Runtime Version Source of Truth: `js/version.js`

### 現在Version

- VReview: **v0.5.1**
- Detector: **v0.5.0**
- Feedback Package: **v5**
- Storage Schema: **v1**
- Feedback Handoff Schema: **v1**

Detectorの閾値・Scene判定条件は今回変更していない。Guide準拠の保存・Validation・UI・検証基盤だけを修正したため、Detector精度比較では引き続きDetector v0.5.0として扱う。

---

## 今回確認したGuide

`EliteMay/web-project-guide` の最新GitHub状態を確認し、`README.md` → `START_HERE.md` から既存Webツール改善ルートを選択した。

今回参照した主な正本:

- `docs/00-governance.md`
- `docs/02-architecture.md`
- `docs/03-data-storage.md`
- `docs/04-ui-ux-accessibility.md`
- `docs/05-performance-reliability.md`
- `docs/06-security.md`
- `docs/07-testing-quality.md`
- `docs/08-github-pages.md`
- `docs/09-maintenance.md`
- `docs/10-project-management.md`
- `docs/12-project-profiles.md`
- `docs/13-dependencies-assets.md`
- `catalog/failures.md`
- `templates/QUALITY_CHECKLIST.md`

GuideでVReview自身の過去失敗として記載されているF-001 / F-004 / F-005 / F-010 / F-011 / F-012も再確認した。

---

## 変更内容

### 1. Project Source of Truthを明文化

新規:

- `project-meta.json`
- `SPEC.md`

`project-meta.json`へGuide Version・Profiles・Runtime Version・Storage Schema・AI Handoff Schema・GitHub Pages base pathを記録した。

READMEは現在の使い方と重要事項へ縮小し、細かい現行仕様は`SPEC.md`、変更履歴と確認状態は本ファイルへ分離した。

### 2. 保存形式をSchema Version付きへ変更

`js/storage.js`へversioned envelopeを追加。

```text
__type: vreview-storage
schemaVersion: 1
revision
updatedAt
data
```

互換性:

- v0.5.0以前のplain Array / Object形式はそのまま読み込める
- 旧データを一括削除しない
- 不正JSONを読んだ場合、可能ならRecovery keyへrawを保存して元keyを残す

### 3. Draft Backup / Restore

同じ動画に既存Draftがあり「新規開始」を選んだ場合、以前はそのDraftを削除していた。

v0.5.1では:

```text
既存Draft
↓
Backupへコピー
↓
新規開始
```

とする。

main Draftがない状態でBackupが残っている場合は、同じ動画を選んだ時にBackup復元を確認する。

### 4. Scene削除Undo

Scene削除後に`元に戻す`を表示し、直前のSceneを復元可能にした。

破壊操作を即時確定するだけの構成を避けた。

### 5. Storage失敗を無言で成功扱いしない

localStorage read / write / schema / migration / backup失敗時に`vreview:storage-error`を発行。

New Reviewで保存失敗を画面表示する。

### 6. 複数タブ競合警告

同じ動画FingerprintのDraftが別タブから更新された場合、`storage` eventを使って競合警告を出す。

完全なRevision mergeは実装していないため、現段階では「片方のタブだけで編集」を案内する。

### 7. Detector Test Import Validation

新規:

- `data/detector-feedback-schema.json`

Detector TestはFeedback ZIPを信頼せず、以下を検証してから集計する。

- ZIP size / entry count / JSON size
- 必須ファイル
- `manifest.schema`
- Package Version v4 / v5
- Scene数
- Start / End
- feedback label
- review tier
- source
- confidence
- ZIP内Path
- JSON構文

ZIP解析では画像等の全EntryをMemoryへ保持せず、集計に必要なJSONだけ保持するよう変更した。

### 8. DOMへのImport文字列挿入を安全化

Detector TestのClip名等、Import由来データをHTML文字列へ直接連結する方式を減らし、`textContent`中心で表を組み立てるよう変更した。

Video metadataも`innerHTML`ではなくDOM + `textContent`で表示する。

### 9. Loading / Empty / Error / Recovery State

New Review:

- 動画Loading
- Video format / load Error
- Storage Error
- Draft Restore
- Backup Restore
- Detector Error + 手動Scene継続案内
- Feedback ZIP Error

Detector Test:

- Empty state
- Validation中
- Import Error
- Success集計

を区別する。

### 10. Accessibility / fixed UI再調整

- `:focus-visible`を明示
- `prefers-reduced-motion`対応
- DropzoneをEnter / Spaceでも操作可能
- `aria-live`をError / Progressへ追加
- current navigationへ`aria-current`
- 主要Button / Inputの操作サイズを拡大
- 520px以下のNavigationを1列化
- 低い縦解像度用PC Layoutを追加

常時Version表示は右下fixed overlayを廃止し、Sidebar内へ移動した。主要操作へ重なる可能性を下げた。

PC版New Reviewの既存要求:

```text
左Navigation固定
中央動画固定
右Scene PaneのみScroll
```

は維持している。

### 11. Version / Cache整合

`js/version.js`へ以下を追加:

- `storageSchema`
- `feedbackSchema`
- `guide`

Cache BustingはBuild `20260830-2`へ統一。

### 12. Static Validationを強化

`scripts/validate.mjs`をGuideのQuality baselineへ拡張。

検出対象:

- 必須ファイル
- JSON構文
- Project Guide / Profiles
- Storage / Feedback Schema整合
- HTML local reference
- CSS / JS Cache Versionずれ
- HTML ID重複
- legacy versioned Detector再混入
- Detector / Feedback runtime Versionずれ
- `localhost`
- Windows/file absolute path
- 代表的Secret Token
- public JSONのData URL

### 13. Storage Regression Test追加

新規:

- `tests/storage.test.mjs`

確認内容:

- v0.5.0以前のlegacy Arrayを読み込める
- Schema v1保存 / 読込
- revision増加
- Backup copy
- corrupt JSON Recovery
- write failureがobservable

GitHub Actionsへ追加した。

### 14. Browser Checklist追加

新規:

- `tests/BROWSER_CHECKLIST.md`

Firefox / Chromium、100 / 125 / 150% Zoom、低い縦解像度、固定Pane、Media Codec、Draft Restore、Backup、Feedback ZIP等を実ブラウザで確認するChecklist。

Checklist追加だけではBrowser Validated扱いにしない。

---

## 互換性への影響

### 維持

- GitHub Pages URL
- `index.html`
- `review.html`
- `detector-test.html`
- Detector v0.5.0判定ロジック
- Feedback Package v5出力
- Detector Testのv4 / v5 Package対応
- v0.5.0以前のScene Draft / meta読込

### 変更

新しく保存するScene Draft / meta / last summaryはStorage Schema v1 envelopeになる。

旧形式を読めるためMigrationのための強制削除はしない。

---

## Verification State

### Implemented

- Guide metadata / SPEC
- Storage Schema v1
- legacy Storage read compatibility
- Draft Backup / Restore
- Scene delete Undo
- Storage failure UI
- cross-tab warning
- Feedback Import Validation
- Accessibility改善
- Static validator拡張
- Storage regression test
- Browser manual checklist

### Static Validated

この項目は最新GitHub Actions実行結果を確認後に更新する。

### Browser Validated

**未確認**。

### Real Media Validated

**未確認**。

### User Validated

**未確認**。

---

## Known Issues / 未完了

- Detector v0.5.0の未知クリップ汎化性能
- ace4-1型の重複Scene
- 長い連キルの自動分割
- Death専用検出
- HUD Scale / Aspect Ratio差
- 固定ROI自動キャリブレーション
- 長尺動画Audio解析中のUI responsivenessはさらに改善余地あり
- Timeline Start / End直接ドラッグ
- ChatGPT採点用30 / 60fps Package
- result JSON Import / History / Training

---

## 2026-08-30 VReview v0.5.0 基盤改修 — 要約

v0.4系で積み重なっていたVersioned Detector Patchを`js/detector.js`の単一Pipelineへ統合し、旧Runtimeを本番フォルダから削除した。

同時に以下を実施した。

- Scene stateを検証前に破壊するバグ修正
- Object URL cleanup
- 動画Fingerprint Draft復元
- Detector Cancel
- fixed動画 + 右Pane Scroll
- weak候補UI
- Feedback Package v5
- Detector Test
- Version一元化
- 初期GitHub Actions validation

v0.5.0の詳細変更はGit履歴を正本とし、現在の仕様は`SPEC.md`を参照する。
